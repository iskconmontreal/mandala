{
  "name": "mandir",
  "private": true,
  "scripts": {
    "start": "bundle exec jekyll serve",
    "start:https": "bundle exec jekyll serve --ssl-key .ssl/key.pem --ssl-cert .ssl/cert.pem",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "npx vitest run",
    "test:e2e": "npx playwright test && npx playwright test --config playwright.e2e.config.js && npx playwright test --config playwright.rotation.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "vitest": "^4.1.4"
  }
}
